<core:FragmentDefinition
        xmlns="sap.m"
        xmlns:core="sap.ui.core">
    <MessagePopover
            id="messagePopoverBox"
            items="{message>/}"
            initiallyExpanded="true">
        <MessagePopoverItem
                type="{message>type}"
                title="{message>message}"
                subtitle="{message>additionalText}"
                description="{message>description}"/>
    </MessagePopover>
</core:FragmentDefinition>