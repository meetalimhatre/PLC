TYPE "sap.plc.db.administration::masterdata.tt_currency_conversion" AS TABLE (
		EXCHANGE_RATE_TYPE_ID NVARCHAR(20),
		FROM_CURRENCY_ID NVARCHAR(3),
		TO_CURRENCY_ID NVARCHAR(3),
		FROM_FACTOR INTEGER,
		TO_FACTOR INTEGER,
		RATE DECIMAL(28, 7),
		VALID_FROM DATE,
		_VALID_FROM TIMESTAMP,
		_VALID_TO TIMESTAMP,
		_SOURCE TINYINT,
		_CREATED_BY NVARCHAR(256),
		_VALID_FROM_FIRST_VERSION TIMESTAMP,
		_CREATED_BY_FIRST_VERSION NVARCHAR(256))