TYPE "sap.plc.db.calcengine::calcengine_types.ts_calculate_costing_sheet_overhead" AS TABLE (
    COSTING_SHEET_OVERHEAD_ID INTEGER,
    CREDIT_ACCOUNT_ID NVARCHAR(10),
    CREDIT_FIXED_COST_PORTION INTEGER,
    IS_ROLLED_UP INTEGER,
    _VALID_FROM TIMESTAMP,
    _VALID_TO TIMESTAMP,
    USE_DEFAULT_FIXED_COST_PORTION INTEGER
)
