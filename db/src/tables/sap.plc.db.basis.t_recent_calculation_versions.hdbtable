COLUMN TABLE "sap.plc.db::basis.t_recent_calculation_versions" (
  CALCULATION_VERSION_ID INTEGER NOT NULL,
  USER_ID NVARCHAR(256) NOT NULL,
  LAST_USED_ON TIMESTAMP NOT NULL,
  PRIMARY KEY(CALCULATION_VERSION_ID, USER_ID)
)