COLUMN TABLE "sap.plc.db::basis.t_lifecycle_period_value" (
  RULE_ID INTEGER NOT NULL,
  LIFECYCLE_PERIOD_FROM INTEGER NOT NULL,
  VALUE DECIMAL(28, 7) NOT NULL,
  PRIMARY KEY(RULE_ID, LIFECYCLE_PERIOD_FROM)
)