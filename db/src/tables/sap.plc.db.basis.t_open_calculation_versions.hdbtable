COLUMN TABLE "sap.plc.db::basis.t_open_calculation_versions" (
  SESSION_ID NVARCHAR(50) NOT NULL,
  CALCULATION_VERSION_ID INTEGER NOT NULL,
  CONTEXT NVARCHAR(35) NOT NULL DEFAULT 'calculation_version',
  IS_WRITEABLE TINYINT NOT NULL,
  PRIMARY KEY(SESSION_ID, CALCULATION_VERSION_ID, CONTEXT)
)