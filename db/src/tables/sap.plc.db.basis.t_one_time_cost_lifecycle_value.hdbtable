COLUMN TABLE "sap.plc.db::basis.t_one_time_cost_lifecycle_value" (
  ONE_TIME_COST_ID INTEGER NOT NULL,
  CALCULATION_ID INTEGER NOT NULL,
  LIFECYCLE_PERIOD_FROM INTEGER NOT NULL,
  VALUE DECIMAL(28, 7) NOT NULL,
  PRIMARY KEY(ONE_TIME_COST_ID, CALCULATION_ID, LIFECYCLE_PERIOD_FROM)
)